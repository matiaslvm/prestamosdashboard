{
  "name": "prestamos-app",
  "version": "1.0.0",
  "description": "Sistema de gestión de préstamos - Full Stack",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "create-user": "node supabase/create-user.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2"
  }
}
